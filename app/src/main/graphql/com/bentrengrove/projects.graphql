query ProjectsQuery($owner: String!, $name: String!) {
    repository(owner: $owner, name: $name) {
        id
        name
        projects(first: 5) {
            totalCount
            nodes {
                id
                name
                number
            }
        }
    }
}